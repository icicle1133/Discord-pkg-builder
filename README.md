a simple discord package builder for arch and rpm systems.

# arch

1. chmod +x install-deps.sh build.sh
2. ./install-deps.sh
3. ./build.sh

# rpm

1. chmod +x build-rpm.sh
2. ./build-rpm.sh

use stable, canary, or ptb as the first argument.
